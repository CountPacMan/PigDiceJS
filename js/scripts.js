var Player = {
  name: null,
  score: 0,
  turnScore: 0,

  addScore: function() {
    // add score
    this.score += this.turnScore;
    // test for win
  }
};

function roll() {
  // roll dice 1d6
};

jQuery(document).ready(function() {
  $("#").focus();
  $("#").submit(function(event) {
    event.preventDefault();

  });
});
